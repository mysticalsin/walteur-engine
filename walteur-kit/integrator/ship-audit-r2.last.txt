blocker: integrator provenance still does not enforce the claimed thread bind; `check_provenance()` only compares `$SD/thread_id` when it is non-empty, so a forged SHIP last.txt plus >200B events.jsonl carrying any uuid passes with no thread_id file / walteur-kit/hooks/integrator-audit-gate.sh:105-130
blocker: integrator provenance still accepts hand-forged event streams as "real rf-codex" proof; the gate checks size + uuid string only, not event schema, driver-produced metadata, command sandbox, model, or read-only evidence / walteur-kit/hooks/integrator-audit-gate.sh:113-130
blocker: DEGRADED driver_log is not constrained "under the statedir" as claimed; absolute paths are accepted, so any external readable file with a matching text signature can evidence an outage / walteur-kit/hooks/integrator-audit-gate.sh:165-176
blocker: DEGRADED still proves only a matching log string, not a real failed rf-codex run; `grep -qaiE "usage limit|requires a newer version|not logged in|401"` can be satisfied by arbitrary text with no exit status or driver invocation binding / walteur-kit/hooks/integrator-audit-gate.sh:170-176
blocker: ledger disclosure says the five eval selftests are OneDrive-dehydrated placeholders whose bytes time out, but on current disk all five are readable nonzero files via `ls -l`/`wc -c`; that causal claim has no checkable basis in this audit / walteur-kit/release-ledger.json:5
blocker: release-ledger-lint is still RED against the live machine report, not just pending; `release-ledger-report.json` fails `aggregate-report` because `selftest-report.json` is FAIL 238/5/0 while ledger aggregate_proof claims 241/0/0 / walteur-kit/release-ledger-report.json / walteur-kit/selftest-report.json:1-10 / walteur-kit/release-ledger.json:6-12
risk: excellence-loop-gate cap path now permits cap exit with no residuals when final round is all_green, while registry evidence still describes "cap with residual deductions PRESENTED"; this is a spec/registry mismatch and can bypass the residual-presentation law at cap / walteur-kit/hooks/excellence-loop-gate.sh:145-154 / walteur-kit/gate-registry.json:1329-1335
risk: `ship-gate` still treats every non-2 dispatched gate exit as SKIP/non-blocking; with bypass envs returning 0, HARD remains dependent on environment discipline and trace review, not mechanical impossibility / walteur-kit/hooks/ship-gate.sh:137-153 / walteur-kit/hooks/integrator-audit-gate.sh:195 / walteur-kit/hooks/excellence-loop-gate.sh:87
nit: WALTEUR-builder-CLAUDE.md v10.20 matrix row still says integrator-audit-gate selftests are 16/16, while `selftest.sh` expects 20/20 and the hook prints its own computed total / WALTEUR-builder-CLAUDE.md:668 / walteur-kit/selftest.sh:1909 / walteur-kit/hooks/integrator-audit-gate.sh:373
VERDICT: REVISE