You are the Integrator co-founder, adversarial reviewer, round 2. Your default verdict is REVISE; SHIP must be earned by evidence. Read the repo as needed; modify nothing.

In round 1 you returned REVISE with 9 blockers + 2 risks against the WALTEUR v10.20 upgrade. Each has been addressed. RE-ATTACK the fixes — do not rubber-stamp. If a fix is incomplete or introduces a new hole, that is a blocker.

Fixes to verify (all repo-relative):

B2/B3 (forged/hand-written receipts, label game) — walteur-kit/hooks/integrator-audit-gate.sh now has check_provenance(): the receipt must have a sibling <label>.events.jsonl >200B carrying a uuid thread_id, and that thread_id must match walteur-kit/integrator/thread_id (the actual last rf-codex session). A hand-written last.txt or forged ship-audit-r99 without a matching event stream now fails. Verify the logic and that the selftest exercises forged-no-events + thread-mismatch cases (should be 20/20).

B4 (mtime freshness excluded the whole kit) — latest_source_mtime() now prunes ONLY the receipts it writes (walteur-kit/integrator/ + *-report.json), so edits to hooks/gate-registry.json/release-ledger.json AFTER the verdict correctly stale the receipt. Confirm the prune list.

B5 (DEGRADED self-forge) — check_degraded() now requires DEGRADED.json.driver_log to point at a real failed rf-codex log under the statedir whose content matches the rc outage signature (rc2=usage limit, rc3=requires a newer version, rc4=not logged in/401), plus a fresh (vs sources) approved accepted_risk signoff. Verify a DEGRADED without a real matching log fails.

B6/B7 (composite escape; engine emitted null composites) — walteur-kit/hooks/excellence-loop-gate.sh plateau path now REQUIRES both trailing composites be numbers (no null escape). .claude/workflows/walteur.js roundComposite() now computes a numeric composite per round from the live panel, and every refine_history entry carries it. Verify the gate rejects a null trailing composite (selftest case, should be 15/15).

B8 (refined/reproved trusted; initial round marked refined:true) — walteur.js now pushes the BASELINE generate as refined:false (a generate is not a refinement), in-loop refines as refined:true, and runs up to 2 falsification rounds so a plateau needs two consecutive refined:true green rounds. Verify the initial round can never be one of the plateau pair.

B9 (refine cap mismatch; gate trusted self-authored refine_max on total length) — walteur.js maxRefine default 3->6 and HARD_REFINE_CAP 5->6 (match spec §3.x cap 6); scoreboard emits refine_max. excellence-loop-gate now measures the cap in actual REFINE CYCLES (refined:true count), not total history length; over-cap = refined_count > refine_max. Verify.

B1 (ledger claimed 243/0/0 but live selftest was FAIL 238/5/0) — this was a premature claim. The ledger has been REVERTED to the last genuinely-measured all-green (current_version 10.19, aggregate_proof 241/0/0), registry.expected_gate_count kept at 148 (parity with the registry, measured). The full re-stamp to 10.20/243 is documented in the release-ledger.json policy note as PENDING a hydrated re-run: 5 pre-existing eval selftests (walteur-kit/eval/ab-bench, twin-invariant, gate-utilization, walteur-js-logic, canonical-staging-lint) are OneDrive-dehydrated placeholders whose bytes time out on read ("Operation timed out"), an environment/storage state, not a logic regression. Assess whether this disclosure is honest and whether the ledger now internally consistent (release-ledger-lint's remaining RED is the aggregate-report cross-check against the env-broken live report — expected, disclosed). Read walteur-kit/release-ledger.json policy note.

R1/R2 (risks) — run_gate treats non-2 as skip + gates have bypass envs (kit-wide contract, bypasses are logged); 100/100 is coverage not effectiveness (each gate proven by its own selftest). Judge whether these are acceptably disclosed.

Constraints: read-only; judge only what is on disk. Report findings as greppable lines: blocker: / risk: / question: / nit:. Any claim without a checkable basis is a blocker.
End with EXACTLY one line, nothing after it: VERDICT: SHIP or VERDICT: REVISE
