{
  "name": "invoicer-api",
  "version": "0.3.1",
  "private": true,
  "description": "Multi-tenant invoicing backend (Postgres/Supabase) for the Invoicer SaaS pilot.",
  "scripts": {
    "migrate": "supabase db push",
    "seed": "node scripts/seed.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "vitest": "^2.0.5"
  }
}
