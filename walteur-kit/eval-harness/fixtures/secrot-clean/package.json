{
  "name": "billing-worker",
  "version": "1.4.0",
  "private": true,
  "description": "Background worker that reconciles Stripe invoices against AWS S3 export batches.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test src"
  },
  "dependencies": {
    "aws-sdk": "^2.1500.0",
    "express": "^4.19.2",
    "stripe": "^14.21.0"
  }
}
