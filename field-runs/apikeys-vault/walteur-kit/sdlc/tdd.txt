TDD — core.test.mjs and the HTTP test/ suite were written to assert deny-by-default + rotation-changes-hash + raw-never-stored + erasure + audit invariants; the implementation is validated by them, not by self-assertion.
