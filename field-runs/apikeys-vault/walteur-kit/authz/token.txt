apikeys-vault authz evidence: token — token policy — control-plane tokens are env-injected via WALTEUR_TENANT_TOKENS only; never committed; rotated per ops/secrets-policy.json (90d max age); redacted to *** in every log line. Issued tenant API keys are stored as sha256 hashes only.
