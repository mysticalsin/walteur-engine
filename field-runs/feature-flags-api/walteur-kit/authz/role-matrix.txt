feature-flags authz evidence: role-matrix — single tenant role; every /flags + /admin/erase op requires X-Tenant + Bearer; see server.mjs authenticate(). Proven by test/api.test.mjs + test/cross-tenant.test.mjs.
